{
  "name": "node-attendance-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && cd .. && cd frontend && npm install && cd ..",
    "build": "cd frontend && npm run build"
  }
}
